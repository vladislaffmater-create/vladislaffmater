На главной странице используются два фото (блок «До» / «После»):

- before.png — фото «До» (участок до покоса)
- after.png  — фото «После» (участок после покоса)

Скопируй сюда свои два изображения с этими именами.
Если файлы в другом формате (например .jpg), переименуй в .png или замени в index.html расширение на .jpg.
